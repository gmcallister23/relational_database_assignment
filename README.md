This file creates a small database using ORM methods and classes.  

The file queries specific information, deletes users and has the ability to clear the database when you run the program to reload the information.
